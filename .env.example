VITE_API_BASE_URL=http://localhost:8000
# Mirrors the backend's REQUIRE_CONFIRMED_UNIT_FOR_SHIPMENT — leave unset/false
# until the POS confirm flow is proven in production.
VITE_REQUIRE_CONFIRMED_UNIT=false
